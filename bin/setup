#!/bin/bash -eu

case "$1" in
  -v|--version)
    version="$2"
esac

# Copy the version specific files to perl directory
shopt -s dotglob
mkdir -p $OPENSHIFT_PERLV2_DIR/etc
cp -r $OPENSHIFT_PERLV2_DIR/versions/$version/etc/* $OPENSHIFT_PERLV2_DIR/etc
mkdir -p $OPENSHIFT_HOMEDIR/.cpanm
